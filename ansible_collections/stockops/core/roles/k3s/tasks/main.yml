- name: Sanity ping from role
  ansible.builtin.debug:
    msg: "k3s role loaded from {{ role_path }}"

# Stage A must run first; it discovers capabilities on controller vs delegate
- name: Stage A | Gather environment info
  ansible.builtin.include_tasks: 10_gather_info.yml

# Stage B consumes Stage A facts and applies or dry-run-validates the staged manifests
- name: Stage B | Apply manifests
  ansible.builtin.include_tasks: 20_apply_manifests.yml







